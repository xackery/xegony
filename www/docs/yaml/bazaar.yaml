get:
  tags: ["Bazaar"]
  description: List all Bazaar entries
  operationId: listBazaar
  parameters:
    - in: query
      type: string
      name: format
      enum: [json, xml, yaml]
      default: json
  responses:
    200:
      description: '200 / OK'
      schema:
        type: array
        items:
          $ref: '#/definitions/Bazaar'
post:
  tags: ["Bazaar"]
  description: Create new Bazaar entry
  operationId: createBazaar
  security:
    - OauthSecurity: [admin]
  parameters: 
    - in: query
      type: string
      name: format
      enum: [json, xml, yaml]
      default: json       
    - in: body
      name: body
      description: Bazaar
      required: true
      schema:
        $ref: '#/definitions/Bazaar'
  responses:
    200:
      description: '200 / OK'
      schema:
        $ref: '#/definitions/Bazaar'