get:
  tags: ["LootTable"]
  description: List all LootTable entries
  operationId: listLootTable
  parameters:
    - in: query
      type: string
      name: format
      enum: [json, xml, yaml]
      default: json
  responses:
    200:
      description: '200 / OK'
      schema:
        type: array
        items:
          $ref: '#/definitions/LootTable'
post:
  tags: ["LootTable"]
  description: Create new LootTable entry
  operationId: createLootTable
  security:
    - OauthSecurity: [admin]
  parameters: 
    - in: query
      type: string
      name: format
      enum: [json, xml, yaml]
      default: json       
    - in: body
      name: body
      description: LootTable
      required: true
      schema:
        $ref: '#/definitions/LootTable'
  responses:
    200:
      description: '200 / OK'
      schema:
        $ref: '#/definitions/LootTable'