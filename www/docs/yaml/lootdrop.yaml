get:
  tags: ["LootDrop"]
  description: List all LootDrop entries
  operationId: listLootDrop
  parameters:
    - in: query
      type: string
      name: format
      enum: [json, xml, yaml]
      default: json
  responses:
    200:
      description: '200 / OK'
      schema:
        type: array
        items:
          $ref: '#/definitions/LootDrop'
post:
  tags: ["LootDrop"]
  description: Create new LootDrop entry
  operationId: createLootDrop
  security:
    - OauthSecurity: [admin]
  parameters: 
    - in: query
      type: string
      name: format
      enum: [json, xml, yaml]
      default: json       
    - in: body
      name: body
      description: LootDrop
      required: true
      schema:
        $ref: '#/definitions/LootDrop'
  responses:
    200:
      description: '200 / OK'
      schema:
        $ref: '#/definitions/LootDrop'