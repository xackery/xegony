get:
  tags: ["LootDrop"]
  description: Details about a specific lootDrop
  operationId: getLootDrop
  parameters:
    - in: path
      name: lootDropID
      required: true
      description: ID
      type: integer
    - in: query
      type: string
      name: format
      enum: [json, xml, yaml]
      default: json
  responses:
    200:
      description: '200 / OK'
      schema:
        $ref: '#/definitions/LootDrop'
put:
  tags: ["LootDrop"]
  description: Edit LootDrop
  operationId: editLootDrop
  security:
    - OauthSecurity: [moderator, admin]
  parameters:
    - in: path
      name: lootDropID
      required: true
      description: ID
      type: integer
    - in: body
      name: body
      description: LootDrop
      required: true
      schema:
        $ref: '#/definitions/LootDrop'
    - in: query
      type: string
      name: format
      enum: [json, xml, yaml]
      default: json
  responses:
    200:
      description: '200 / OK'
      schema:
        $ref: '#/definitions/LootDrop'    
delete:
  tags: ["LootDrop"]
  description: Delete lootDrop
  operationId: deleteLootDrop
  security:
    - OauthSecurity: [admin]
  parameters:
    - in: path
      name: lootDropID
      required: true
      description: ID of lootDrop
      type: integer
    - in: query
      type: string
      name: format
      enum: [json, xml, yaml]
      default: json
  responses:
    200:
      description: '200 / OK'
      schema:
        $ref: '#/definitions/LootDrop'