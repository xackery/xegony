get:
  tags: ["NpcLoot"]
  description: Details about a specific npcLoot
  operationId: getNpcLoot
  parameters:
    - in: path
      name: npcLootID
      required: true
      description: ID
      type: integer
    - in: query
      type: string
      name: format
      enum: [json, xml, yaml]
      default: json
  responses:
    200:
      description: '200 / OK'
      schema:
        $ref: '#/definitions/NpcLoot'
put:
  tags: ["NpcLoot"]
  description: Edit NpcLoot
  operationId: editNpcLoot
  security:
    - OauthSecurity: [moderator, admin]
  parameters:
    - in: path
      name: npcLootID
      required: true
      description: ID
      type: integer
    - in: body
      name: body
      description: NpcLoot
      required: true
      schema:
        $ref: '#/definitions/NpcLoot'
    - in: query
      type: string
      name: format
      enum: [json, xml, yaml]
      default: json
  responses:
    200:
      description: '200 / OK'
      schema:
        $ref: '#/definitions/NpcLoot'    
delete:
  tags: ["NpcLoot"]
  description: Delete npcLoot
  operationId: deleteNpcLoot
  security:
    - OauthSecurity: [admin]
  parameters:
    - in: path
      name: npcLootID
      required: true
      description: ID of npcLoot
      type: integer
    - in: query
      type: string
      name: format
      enum: [json, xml, yaml]
      default: json
  responses:
    200:
      description: '200 / OK'
      schema:
        $ref: '#/definitions/NpcLoot'