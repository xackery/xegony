get:
  tags: ["Activity"]
  description: Details about a specific activity
  operationId: getActivity
  parameters:
    - in: path
      name: activityId
      required: true
      description: ID
      type: integer
    - in: query
      type: string
      name: format
      enum: [json, xml, yaml]
      default: json
  responses:
    200:
      description: '200 / OK'
      schema:
        $ref: '#/definitions/Activity'
put:
  tags: ["Activity"]
  description: Edit Activity
  operationId: editActivity
  security:
    - OauthSecurity: [moderator, admin]
  parameters:
    - in: path
      name: activityId
      required: true
      description: ID
      type: integer
    - in: body
      name: body
      description: Activity
      required: true
      schema:
        $ref: '#/definitions/Activity'
    - in: query
      type: string
      name: format
      enum: [json, xml, yaml]
      default: json
  responses:
    200:
      description: '200 / OK'
      schema:
        $ref: '#/definitions/Activity'    
delete:
  tags: ["Activity"]
  description: Delete activity
  operationId: deleteActivity
  security:
    - OauthSecurity: [admin]
  parameters:
    - in: path
      name: activityId
      required: true
      description: ID of activity
      type: integer
    - in: query
      type: string
      name: format
      enum: [json, xml, yaml]
      default: json
  responses:
    200:
      description: '200 / OK'
      schema:
        $ref: '#/definitions/Activity'