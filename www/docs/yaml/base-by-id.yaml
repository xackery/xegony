get:
  tags: ["Base"]
  description: Details about a specific base
  operationId: getBase
  parameters:
    - in: path
      name: class
      required: true
      description: class
      type: integer
    - in: path
      name: level
      required: true
      description: level
      type: integer
    - in: query
      type: string
      name: format
      enum: [json, xml, yaml]
      default: json
  responses:
    200:
      description: '200 / OK'
      schema:
        $ref: '#/definitions/Base'
put:
  tags: ["Base"]
  description: Edit Base
  operationId: editBase
  security:
    - OauthSecurity: [moderator, admin]
  parameters:
    - in: path
      name: class
      required: true
      description: class
      type: integer
    - in: path
      name: level
      required: true
      description: level
      type: integer
    - in: body
      name: body
      description: Base
      required: true
      schema:
        $ref: '#/definitions/Base'
    - in: query
      type: string
      name: format
      enum: [json, xml, yaml]
      default: json
  responses:
    200:
      description: '200 / OK'
      schema:
        $ref: '#/definitions/Base'    
delete:
  tags: ["Base"]
  description: Delete base
  operationId: deleteBase
  security:
    - OauthSecurity: [admin]
  parameters:
    - in: path
      name: class
      required: true
      description: class
      type: integer
    - in: path
      name: level
      required: true
      description: level
      type: integer
    - in: query
      type: string
      name: format
      enum: [json, xml, yaml]
      default: json
  responses:
    200:
      description: '200 / OK'
      schema:
        $ref: '#/definitions/Base'          