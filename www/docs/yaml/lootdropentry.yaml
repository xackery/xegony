get:
  tags: ["LootDropEntry"]
  description: List all LootDropEntry entries
  operationId: listLootDropEntry
  parameters:
    - in: query
      type: string
      name: format
      enum: [json, xml, yaml]
      default: json
  responses:
    200:
      description: '200 / OK'
      schema:
        type: array
        items:
          $ref: '#/definitions/LootDropEntry'
post:
  tags: ["LootDropEntry"]
  description: Create new LootDropEntry entry
  operationId: createLootDropEntry
  security:
    - OauthSecurity: [admin]
  parameters: 
    - in: query
      type: string
      name: format
      enum: [json, xml, yaml]
      default: json       
    - in: body
      name: body
      description: LootDropEntry
      required: true
      schema:
        $ref: '#/definitions/LootDropEntry'
  responses:
    200:
      description: '200 / OK'
      schema:
        $ref: '#/definitions/LootDropEntry'