get:
  tags: ["NpcLoot"]
  description: List all NpcLoot entries
  operationId: listNpcLoot
  parameters:
    - in: query
      type: string
      name: format
      enum: [json, xml, yaml]
      default: json
  responses:
    200:
      description: '200 / OK'
      schema:
        type: array
        items:
          $ref: '#/definitions/NpcLoot'
post:
  tags: ["NpcLoot"]
  description: Create new NpcLoot entry
  operationId: createNpcLoot
  security:
    - OauthSecurity: [admin]
  parameters: 
    - in: query
      type: string
      name: format
      enum: [json, xml, yaml]
      default: json       
    - in: body
      name: body
      description: NpcLoot
      required: true
      schema:
        $ref: '#/definitions/NpcLoot'
  responses:
    200:
      description: '200 / OK'
      schema:
        $ref: '#/definitions/NpcLoot'