get:
  tags: ["LootTableEntry"]
  description: List all LootTableEntry entries
  operationId: listLootTableEntry
  parameters:
    - in: query
      type: string
      name: format
      enum: [json, xml, yaml]
      default: json
  responses:
    200:
      description: '200 / OK'
      schema:
        type: array
        items:
          $ref: '#/definitions/LootTableEntry'
post:
  tags: ["LootTableEntry"]
  description: Create new LootTableEntry entry
  operationId: createLootTableEntry
  security:
    - OauthSecurity: [admin]
  parameters: 
    - in: query
      type: string
      name: format
      enum: [json, xml, yaml]
      default: json       
    - in: body
      name: body
      description: LootTableEntry
      required: true
      schema:
        $ref: '#/definitions/LootTableEntry'
  responses:
    200:
      description: '200 / OK'
      schema:
        $ref: '#/definitions/LootTableEntry'