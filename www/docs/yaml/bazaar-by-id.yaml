get:
  tags: ["Bazaar"]
  description: Details about a specific bazaar
  operationId: getBazaar
  parameters:
    - in: path
      name: bazaarID
      required: true
      description: ID
      type: integer
    - in: query
      type: string
      name: format
      enum: [json, xml, yaml]
      default: json
  responses:
    200:
      description: '200 / OK'
      schema:
        $ref: '#/definitions/Bazaar'
put:
  tags: ["Bazaar"]
  description: Edit Bazaar
  operationId: editBazaar
  security:
    - OauthSecurity: [moderator, admin]
  parameters:
    - in: path
      name: bazaarID
      required: true
      description: ID
      type: integer
    - in: body
      name: body
      description: Bazaar
      required: true
      schema:
        $ref: '#/definitions/Bazaar'
    - in: query
      type: string
      name: format
      enum: [json, xml, yaml]
      default: json
  responses:
    200:
      description: '200 / OK'
      schema:
        $ref: '#/definitions/Bazaar'    
delete:
  tags: ["Bazaar"]
  description: Delete bazaar
  operationId: deleteBazaar
  security:
    - OauthSecurity: [admin]
  parameters:
    - in: path
      name: bazaarID
      required: true
      description: ID of bazaar
      type: integer
    - in: query
      type: string
      name: format
      enum: [json, xml, yaml]
      default: json
  responses:
    200:
      description: '200 / OK'
      schema:
        $ref: '#/definitions/Bazaar'