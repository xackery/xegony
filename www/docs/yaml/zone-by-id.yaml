get:
  tags: ["Zone"]
  description: Details about a specific zone
  operationId: getZone
  parameters:
    - in: path
      name: zoneId
      required: true
      description: ID
      type: integer
    - in: query
      type: string
      name: format
      enum: [json, xml, yaml]
      default: json
  responses:
    200:
      description: '200 / OK'
      schema:
        $ref: '#/definitions/Zone'
put:
  tags: ["Zone"]
  description: Edit Zone
  operationId: editZone
  security:
    - OauthSecurity: [moderator, admin]
  parameters:
    - in: path
      name: zoneId
      required: true
      description: ID
      type: integer
    - in: body
      name: body
      description: Zone
      required: true
      schema:
        $ref: '#/definitions/Zone'
    - in: query
      type: string
      name: format
      enum: [json, xml, yaml]
      default: json
  responses:
    200:
      description: '200 / OK'
      schema:
        $ref: '#/definitions/Zone'    
delete:
  tags: ["Zone"]
  description: Delete zone
  operationId: deleteZone
  security:
    - OauthSecurity: [admin]
  parameters:
    - in: path
      name: zoneId
      required: true
      description: ID of zone
      type: integer
    - in: query
      type: string
      name: format
      enum: [json, xml, yaml]
      default: json
  responses:
    200:
      description: '200 / OK'
      schema:
        $ref: '#/definitions/Zone'